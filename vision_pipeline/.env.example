# General Pipeline Config
OUTPUT_DIR="data"
MAX_WORKERS=4
PIPELINE_STAGES=["crawl", "filter", "detect", "verify"]

# Google Crawler Config
GOOGLE_API_KEY=""
GOOGLE_CX=""
GOOGLE_NUM_RESULTS=100

# Naver Crawler Config
NAVER_CLIENT_ID=""
NAVER_CLIENT_SECRET=""
NAVER_NUM_RESULTS=100

# Filter Config
FILTER_DEDUP_METHOD="phash"
FILTER_DEDUP_THRESHOLD=5
FILTER_MIN_WIDTH=256
FILTER_MIN_HEIGHT=256
FILTER_MIN_FACE_SIZE=50

# Detector Config
YOLO_MODEL_PATH="vision_pipeline/models/yolov8n.pt"

# LLM / verify Config
CLASSIFIER_MODEL="openai/clip-vit-base-patch32"
CLASSIFIER_POSITIVE_PROMPTS=["an image of hatsune miku"]
CLASSIFIER_NEGATIVE_PROMPTS=["text", "low quality", "blurred"]
CLASSIFIER_THRESHOLD=0.25
